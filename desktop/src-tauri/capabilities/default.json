{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local optimizer window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
